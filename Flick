--[[
    FLICK AIMLOCK + ESP v5 – Fixed Dropdowns + Smooth Drag
    - All dropdowns show all options correctly
    - Smooth spring drag animation (window follows with easing)
    - Feather Hub style UI
    - Press ] to open/close, E to toggle aimbot
]]

local Players = game:GetService("Players")
local RunService = game:GetService("RunService")
local UserInputService = game:GetService("UserInputService")
local TweenService = game:GetService("TweenService")
local CoreGui = game:GetService("CoreGui")
local Camera = workspace.CurrentCamera
local LocalPlayer = Players.LocalPlayer

-- ==================== SETTINGS ====================
local settings = {
    aimbotEnabled = false,
    aimKey = Enum.KeyCode.E,
    targetMode = "mouse",
    fovDistance = 200,
    smoothing = 0,
    aimPart = "Head",
    espEnabled = true,
    espColorPreset = "White",
    espColor = Color3.fromRGB(255, 255, 255),
    espTransparency = 0.6,
    tracersEnabled = true,
    highlightsEnabled = true,
    uiVisible = true,
}

local colorPresets = {
    White  = Color3.fromRGB(255, 255, 255),
    Red    = Color3.fromRGB(255, 70, 70),
    Green  = Color3.fromRGB(70, 255, 70),
    Blue   = Color3.fromRGB(70, 150, 255),
    Yellow = Color3.fromRGB(255, 255, 100),
    Purple = Color3.fromRGB(200, 100, 255),
    Orange = Color3.fromRGB(255, 140, 70),
}

-- ==================== INTERNAL ====================
local waitingForKeybind = false
local tracers = {}
local highlights = {}
local screenGui = nil
local uiElements = {}

-- ==================== ENEMY DETECTION ====================
local function getEnemyCharacters()
    local enemies = {}
    local myChar = LocalPlayer.Character
    for _, obj in pairs(workspace:GetChildren()) do
        if obj:IsA("Model") and obj:FindFirstChild("Humanoid") and obj:FindFirstChild("HumanoidRootPart") then
            if obj ~= myChar then
                local humanoid = obj:FindFirstChild("Humanoid")
                if humanoid and humanoid.Health > 0 then
                    table.insert(enemies, obj)
                end
            end
        end
    end
    return enemies
end

local function getHitboxPosition(character)
    local part = character:FindFirstChild(settings.aimPart)
    if not part then part = character:FindFirstChild("HumanoidRootPart") end
    return part and part.Position or nil
end

local function getClosestToMouse()
    local closest = nil
    local minScreenDist = math.huge
    local mousePos = UserInputService:GetMouseLocation()
    local cameraPos = Camera.CFrame.Position

    for _, enemy in ipairs(getEnemyCharacters()) do
        local pos = getHitboxPosition(enemy)
        if pos then
            local distWorld = (pos - cameraPos).Magnitude
            if distWorld <= settings.fovDistance then
                local screenPoint, onScreen = Camera:WorldToViewportPoint(pos)
                if onScreen then
                    local screenDist = (Vector2.new(screenPoint.X, screenPoint.Y) - mousePos).Magnitude
                    if screenDist < minScreenDist then
                        minScreenDist = screenDist
                        closest = enemy
                    end
                end
            end
        end
    end
    return closest
end

local function getClosestToPlayer()
    local closest = nil
    local minDist = settings.fovDistance
    local cameraPos = Camera.CFrame.Position

    for _, enemy in ipairs(getEnemyCharacters()) do
        local pos = getHitboxPosition(enemy)
        if pos then
            local dist = (pos - cameraPos).Magnitude
            if dist < minDist then
                minDist = dist
                closest = enemy
            end
        end
    end
    return closest
end

local function getTarget()
    if settings.targetMode == "mouse" then
        return getClosestToMouse()
    else
        return getClosestToPlayer()
    end
end

local function aimAt(targetChar)
    local aimPart = targetChar:FindFirstChild(settings.aimPart) or targetChar:FindFirstChild("HumanoidRootPart")
    if not aimPart then return end
    local targetPos = aimPart.Position

    if settings.smoothing <= 0 then
        Camera.CFrame = CFrame.new(Camera.CFrame.Position, targetPos)
    else
        local newCF = CFrame.new(Camera.CFrame.Position, targetPos)
        Camera.CFrame = Camera.CFrame:Lerp(newCF, 1 - settings.smoothing)
    end
end

-- Aimbot loop
RunService.RenderStepped:Connect(function()
    if settings.aimbotEnabled then
        local target = getTarget()
        if target then aimAt(target) end
    end
end)

-- ==================== ESP ====================
local function updateESP()
    if not settings.espEnabled then
        for _, line in pairs(tracers) do pcall(function() line:Remove() end) end
        for _, hl in pairs(highlights) do pcall(function() hl:Destroy() end) end
        tracers = {}; highlights = {}
        return
    end

    for enemy, line in pairs(tracers) do
        if not enemy or not enemy.Parent or (enemy:FindFirstChild("Humanoid") and enemy.Humanoid.Health <= 0) then
            pcall(function() line:Remove() end); tracers[enemy] = nil
        end
    end
    for enemy, hl in pairs(highlights) do
        if not enemy or not enemy.Parent or (enemy:FindFirstChild("Humanoid") and enemy.Humanoid.Health <= 0) then
            pcall(function() hl:Destroy() end); highlights[enemy] = nil
        end
    end

    for _, enemy in ipairs(getEnemyCharacters()) do
        if settings.tracersEnabled and not tracers[enemy] then
            local line = Drawing.new("Line")
            line.Color = settings.espColor
            line.Thickness = 1
            line.Transparency = settings.espTransparency
            line.Visible = true
            tracers[enemy] = line
        end
        if settings.highlightsEnabled and not highlights[enemy] then
            local hl = Instance.new("Highlight")
            hl.FillColor = settings.espColor
            hl.FillTransparency = settings.espTransparency
            hl.OutlineColor = settings.espColor
            hl.OutlineTransparency = 0.3
            hl.DepthMode = Enum.HighlightDepthMode.AlwaysOnTop
            hl.Adornee = enemy
            hl.Parent = enemy
            highlights[enemy] = hl
        end
    end
end

RunService.Heartbeat:Connect(updateESP)

-- Draw tracers
RunService.RenderStepped:Connect(function()
    if not settings.espEnabled or not settings.tracersEnabled then
        for _, line in pairs(tracers) do if line then line.Visible = false end end
        return
    end
    local viewport = Camera.ViewportSize
    local center = Vector2.new(viewport.X / 2, viewport.Y / 2)
    for enemy, line in pairs(tracers) do
        if enemy and enemy.Parent then
            local pos = getHitboxPosition(enemy)
            if pos then
                local vec = Camera:WorldToViewportPoint(pos)
                local screenPos = Vector2.new(vec.X, vec.Y)
                if vec.Z < 0 then screenPos = center + (center - screenPos) end
                screenPos = Vector2.new(math.clamp(screenPos.X, 0, viewport.X), math.clamp(screenPos.Y, 0, viewport.Y))
                line.From = center; line.To = screenPos; line.Visible = true
            else
                line.Visible = false
            end
        else
            line.Visible = false
        end
    end
end)

-- ==================== KEYBIND HANDLER ====================
UserInputService.InputBegan:Connect(function(input, gameProcessed)
    if gameProcessed then return end
    if waitingForKeybind then
        if input.KeyCode ~= Enum.KeyCode.Unknown then
            settings.aimKey = input.KeyCode
            waitingForKeybind = false
            if uiElements.keyButton then uiElements.keyButton.Text = settings.aimKey.Name end
            if uiElements.infoLabel then uiElements.infoLabel.Text = "Open/close UI:  ]\nToggle aimbot:  " .. settings.aimKey.Name end
        end
        return
    end
    if input.KeyCode == settings.aimKey then
        settings.aimbotEnabled = not settings.aimbotEnabled
        if uiElements.aimbotToggle then
            uiElements.aimbotToggle.Text = settings.aimbotEnabled and "ON" or "OFF"
            uiElements.aimbotToggle.BackgroundColor3 = settings.aimbotEnabled and Color3.fromRGB(80, 180, 80) or Color3.fromRGB(180, 60, 60)
        end
    end
    if input.KeyCode == Enum.KeyCode.RightBracket then
        settings.uiVisible = not settings.uiVisible
        if screenGui then screenGui.Enabled = settings.uiVisible end
    end
end)

-- ==================== SMOOTH DRAG (Spring Animation) ====================
local function createSmoothDrag(frame)
    local dragData = {
        active = false,
        startPos = Vector2.new(),
        startFramePos = Vector2.new(),
        currentVel = Vector2.new(),
        targetPos = Vector2.new(),
        connection = nil,
    }

    local function updateDrag()
        if not dragData.active then
            -- Apply spring smoothing
            local delta = dragData.targetPos - Vector2.new(frame.Position.X.Offset, frame.Position.Y.Offset)
            dragData.currentVel = dragData.currentVel * 0.85 + delta * 0.2
            local newX = math.clamp(frame.Position.X.Offset + dragData.currentVel.X, 0, workspace.CurrentCamera.ViewportSize.X - frame.AbsoluteSize.X)
            local newY = math.clamp(frame.Position.Y.Offset + dragData.currentVel.Y, 0, workspace.CurrentCamera.ViewportSize.Y - frame.AbsoluteSize.Y)
            frame.Position = UDim2.new(0, newX, 0, newY)
            dragData.targetPos = Vector2.new(newX, newY)
            if delta.Magnitude < 0.5 and dragData.currentVel.Magnitude < 0.5 then
                dragData.connection:Disconnect()
                dragData.connection = nil
            end
        end
    end

    local titleBar = frame:FindFirstChild("TitleBar") or frame:FindFirstChild("Header") or frame:FindFirstChild("DragHandle")
    if not titleBar then return end

    titleBar.InputBegan:Connect(function(input)
        if input.UserInputType == Enum.UserInputType.MouseButton1 then
            dragData.active = true
            dragData.startPos = input.Position
            dragData.startFramePos = Vector2.new(frame.Position.X.Offset, frame.Position.Y.Offset)
            dragData.currentVel = Vector2.new()
            if dragData.connection then dragData.connection:Disconnect() end
            dragData.connection = RunService.Heartbeat:Connect(updateDrag)
        end
    end)

    titleBar.InputEnded:Connect(function(input)
        if input.UserInputType == Enum.UserInputType.MouseButton1 then
            dragData.active = false
            dragData.targetPos = Vector2.new(frame.Position.X.Offset, frame.Position.Y.Offset)
        end
    end)

    UserInputService.InputChanged:Connect(function(input)
        if dragData.active and input.UserInputType == Enum.UserInputType.MouseMovement then
            local delta = input.Position - dragData.startPos
            local newX = math.clamp(dragData.startFramePos.X + delta.X, 0, workspace.CurrentCamera.ViewportSize.X - frame.AbsoluteSize.X)
            local newY = math.clamp(dragData.startFramePos.Y + delta.Y, 0, workspace.CurrentCamera.ViewportSize.Y - frame.AbsoluteSize.Y)
            frame.Position = UDim2.new(0, newX, 0, newY)
            dragData.targetPos = Vector2.new(newX, newY)
        end
    end)
end

-- ==================== UI (Feather Hub Style) ====================
local function createUI()
    if screenGui then screenGui:Destroy() end
    screenGui = Instance.new("ScreenGui")
    screenGui.Name = "FlickAimUI"
    screenGui.ResetOnSpawn = false
    screenGui.ZIndexBehavior = Enum.ZIndexBehavior.Sibling
    pcall(function() screenGui.Parent = CoreGui end)
    if not screenGui.Parent then screenGui.Parent = LocalPlayer:WaitForChild("PlayerGui") end
    screenGui.Enabled = settings.uiVisible

    local T = {
        bg = Color3.fromRGB(12, 12, 14),
        sidebar = Color3.fromRGB(18, 18, 21),
        card = Color3.fromRGB(24, 24, 28),
        border = Color3.fromRGB(38, 38, 46),
        text = Color3.fromRGB(240, 240, 244),
        muted = Color3.fromRGB(110, 110, 128),
        dim = Color3.fromRGB(28, 28, 33),
        sep = Color3.fromRGB(34, 34, 40),
        header = Color3.fromRGB(15, 15, 18),
    }
    local accent = Color3.fromRGB(88, 101, 242)
    local WIN_W, WIN_H = 420, 340
    local SIDE_W = 64
    local HDR_H = 40

    local function tw(t, s, d) return TweenInfo.new(t or 0.2, s or Enum.EasingStyle.Quart, d or Enum.EasingDirection.Out) end

    -- Main window
    local win = Instance.new("Frame")
    win.Name = "Win"
    win.Size = UDim2.new(0, WIN_W, 0, WIN_H)
    win.Position = UDim2.new(0.5, -WIN_W/2, 0.5, -WIN_H/2)
    win.BackgroundColor3 = T.bg
    win.BorderSizePixel = 0
    win.ClipsDescendants = true
    win.Parent = screenGui
    Instance.new("UICorner", win).CornerRadius = UDim.new(0, 14)
    local winStroke = Instance.new("UIStroke")
    winStroke.Color = T.border
    winStroke.Thickness = 1
    winStroke.Parent = win

    -- Glow
    local winGlow = Instance.new("ImageLabel")
    winGlow.Size = UDim2.new(1, 40, 1, 40)
    winGlow.Position = UDim2.new(0, -20, 0, -20)
    winGlow.BackgroundTransparency = 1
    winGlow.Image = "rbxassetid://1316045217"
    winGlow.ImageColor3 = Color3.fromRGB(60, 65, 100)
    winGlow.ImageTransparency = 0.88
    winGlow.ScaleType = Enum.ScaleType.Slice
    winGlow.SliceCenter = Rect.new(10, 10, 118, 118)
    winGlow.ZIndex = -1
    winGlow.Parent = win

    -- Sidebar
    local sidebar = Instance.new("Frame")
    sidebar.Size = UDim2.new(0, SIDE_W, 1, 0)
    sidebar.BackgroundColor3 = T.sidebar
    sidebar.BorderSizePixel = 0
    sidebar.Parent = win
    Instance.new("UICorner", sidebar).CornerRadius = UDim.new(0, 14)

    local logoPill = Instance.new("Frame")
    logoPill.Size = UDim2.new(0, 30, 0, 30)
    logoPill.Position = UDim2.new(0.5, -15, 0, 8)
    logoPill.BackgroundColor3 = accent
    logoPill.BorderSizePixel = 0
    logoPill.Parent = sidebar
    Instance.new("UICorner", logoPill).CornerRadius = UDim.new(0, 9)
    local logoTxt = Instance.new("TextLabel")
    logoTxt.Size = UDim2.new(1, 0, 1, 0)
    logoTxt.Text = "F"
    logoTxt.TextSize = 14
    logoTxt.Font = Enum.Font.GothamBlack
    logoTxt.TextColor3 = Color3.new(1, 1, 1)
    logoTxt.BackgroundTransparency = 1
    logoTxt.Parent = logoPill

    local sbSep = Instance.new("Frame")
    sbSep.Size = UDim2.new(0.7, 0, 0, 1)
    sbSep.Position = UDim2.new(0.15, 0, 0, 44)
    sbSep.BackgroundColor3 = T.sep
    sbSep.BorderSizePixel = 0
    sbSep.Parent = sidebar

    -- Navigation
    local navCont = Instance.new("Frame")
    navCont.Size = UDim2.new(1, 0, 1, -80)
    navCont.Position = UDim2.new(0, 0, 0, 50)
    navCont.BackgroundTransparency = 1
    navCont.Parent = sidebar
    local navList = Instance.new("UIListLayout")
    navList.SortOrder = Enum.SortOrder.LayoutOrder
    navList.VerticalAlignment = Enum.VerticalAlignment.Top
    navList.HorizontalAlignment = Enum.HorizontalAlignment.Center
    navList.Padding = UDim.new(0, 2)
    navList.Parent = navCont

    local wmName = Instance.new("TextLabel")
    wmName.Size = UDim2.new(1, 0, 0, 12)
    wmName.Position = UDim2.new(0, 0, 1, -36)
    wmName.Text = "feather hub"
    wmName.Font = Enum.Font.GothamBold
    wmName.TextSize = 7
    wmName.TextColor3 = accent
    wmName.BackgroundTransparency = 1
    wmName.Parent = sidebar

    local wmVer = Instance.new("TextLabel")
    wmVer.Size = UDim2.new(1, 0, 0, 10)
    wmVer.Position = UDim2.new(0, 0, 1, -22)
    wmVer.Text = "v1.0"
    wmVer.Font = Enum.Font.Gotham
    wmVer.TextSize = 6
    wmVer.TextColor3 = T.muted
    wmVer.BackgroundTransparency = 1
    wmVer.Parent = sidebar

    -- Header (draggable area)
    local hdr = Instance.new("Frame")
    hdr.Name = "TitleBar"
    hdr.Size = UDim2.new(1, -SIDE_W, 0, HDR_H)
    hdr.Position = UDim2.new(0, SIDE_W, 0, 0)
    hdr.BackgroundColor3 = T.header
    hdr.BorderSizePixel = 0
    hdr.Parent = win
    Instance.new("UICorner", hdr).CornerRadius = UDim.new(0, 14)

    local hdrTitle = Instance.new("TextLabel")
    hdrTitle.Size = UDim2.new(1, -60, 1, 0)
    hdrTitle.Position = UDim2.new(0, 14, 0, 0)
    hdrTitle.Text = "AIMBOT"
    hdrTitle.Font = Enum.Font.GothamBold
    hdrTitle.TextSize = 12
    hdrTitle.TextColor3 = T.text
    hdrTitle.BackgroundTransparency = 1
    hdrTitle.TextXAlignment = Enum.TextXAlignment.Left
    hdrTitle.Parent = hdr

    local closeBtn = Instance.new("TextButton")
    closeBtn.Size = UDim2.new(0, 24, 0, 24)
    closeBtn.Position = UDim2.new(1, -30, 0.5, -12)
    closeBtn.Text = "×"
    closeBtn.Font = Enum.Font.GothamBold
    closeBtn.TextSize = 16
    closeBtn.TextColor3 = T.muted
    closeBtn.BackgroundColor3 = T.dim
    closeBtn.BorderSizePixel = 0
    closeBtn.AutoButtonColor = false
    closeBtn.Parent = hdr
    Instance.new("UICorner", closeBtn).CornerRadius = UDim.new(0, 6)
    closeBtn.MouseEnter:Connect(function() TweenService:Create(closeBtn, tw(), {BackgroundColor3 = Color3.fromRGB(239, 68, 68), TextColor3 = Color3.new(1, 1, 1)}):Play() end)
    closeBtn.MouseLeave:Connect(function() TweenService:Create(closeBtn, tw(), {BackgroundColor3 = T.dim, TextColor3 = T.muted}):Play() end)
    closeBtn.MouseButton1Click:Connect(function() screenGui:Destroy() end)

    -- Apply smooth drag to window
    createSmoothDrag(win)

    -- Content area
    local content = Instance.new("Frame")
    content.Size = UDim2.new(1, -SIDE_W, 1, -HDR_H)
    content.Position = UDim2.new(0, SIDE_W, 0, HDR_H)
    content.BackgroundTransparency = 1
    content.Parent = win

    -- Tabs
    local TABS = {{name = "AIMBOT"}, {name = "ESP"}, {name = "SETTINGS"}}
    local pages, navBtns, activeTab = {}, {}, 1

    local function makePage()
        local pg = Instance.new("ScrollingFrame")
        pg.Size = UDim2.new(1, 0, 1, 0)
        pg.BackgroundTransparency = 1
        pg.BorderSizePixel = 0
        pg.ScrollBarThickness = 3
        pg.ScrollBarImageColor3 = accent
        pg.CanvasSize = UDim2.new(0, 0, 0, 0)
        pg.AutomaticCanvasSize = Enum.AutomaticSize.Y
        pg.ScrollingDirection = Enum.ScrollingDirection.Y
        pg.ElasticBehavior = Enum.ElasticBehavior.Never
        pg.Visible = false
        pg.Parent = content
        local layout = Instance.new("UIListLayout")
        layout.SortOrder = Enum.SortOrder.LayoutOrder
        layout.HorizontalAlignment = Enum.HorizontalAlignment.Center
        layout.Padding = UDim.new(0, 4)
        layout.Parent = pg
        local pad = Instance.new("UIPadding")
        pad.PaddingTop = UDim.new(0, 8)
        pad.PaddingBottom = UDim.new(0, 8)
        pad.PaddingLeft = UDim.new(0, 8)
        pad.PaddingRight = UDim.new(0, 8)
        pad.Parent = pg
        return pg
    end

    for i = 1, #TABS do pages[i] = makePage() end

    local function setTab(idx)
        activeTab = idx
        hdrTitle.Text = TABS[idx].name
        for i, btn in ipairs(navBtns) do
            local on = (i == idx)
            TweenService:Create(btn.bg, tw(0.15), {BackgroundColor3 = on and accent or T.dim, BackgroundTransparency = on and 0.15 or 0}):Play()
            TweenService:Create(btn.lbl, tw(0.15), {TextColor3 = on and accent or T.muted}):Play()
            btn.bar.BackgroundTransparency = on and 0 or 1
            pages[i].Visible = on
        end
    end

    for i, def in ipairs(TABS) do
        local row = Instance.new("Frame")
        row.Size = UDim2.new(1, -4, 0, 28)
        row.BackgroundTransparency = 1
        row.LayoutOrder = i
        row.Parent = navCont
        local bar = Instance.new("Frame")
        bar.Size = UDim2.new(0, 3, 0.5, 0)
        bar.Position = UDim2.new(0, 0, 0.25, 0)
        bar.BackgroundColor3 = accent
        bar.BackgroundTransparency = 1
        bar.BorderSizePixel = 0
        bar.Parent = row
        Instance.new("UICorner", bar).CornerRadius = UDim.new(1, 0)
        local bg = Instance.new("Frame")
        bg.Size = UDim2.new(1, -4, 1, -2)
        bg.Position = UDim2.new(0, 4, 0, 1)
        bg.BackgroundColor3 = T.dim
        bg.BackgroundTransparency = 0
        bg.BorderSizePixel = 0
        bg.Parent = row
        Instance.new("UICorner", bg).CornerRadius = UDim.new(0, 7)
        local lbl = Instance.new("TextLabel")
        lbl.Size = UDim2.new(1, 0, 1, 0)
        lbl.Text = def.name
        lbl.Font = Enum.Font.GothamBold
        lbl.TextSize = 7
        lbl.TextColor3 = T.muted
        lbl.BackgroundTransparency = 1
        lbl.Parent = row
        local hit = Instance.new("TextButton")
        hit.Size = UDim2.new(1, 0, 1, 0)
        hit.BackgroundTransparency = 1
        hit.Text = ""
        hit.Parent = row
        hit.MouseButton1Click:Connect(function() setTab(i) end)
        navBtns[i] = {bg = bg, lbl = lbl, bar = bar}
    end

    -- UI Helpers
    local function makeCard(parent, order)
        local c = Instance.new("Frame")
        c.Size = UDim2.new(1, 0, 0, 0)
        c.AutomaticSize = Enum.AutomaticSize.Y
        c.BackgroundColor3 = T.card
        c.BorderSizePixel = 0
        c.LayoutOrder = order
        c.Parent = parent
        Instance.new("UICorner", c).CornerRadius = UDim.new(0, 10)
        local stroke = Instance.new("UIStroke")
        stroke.Color = T.border
        stroke.Thickness = 1
        stroke.Parent = c
        local layout = Instance.new("UIListLayout")
        layout.SortOrder = Enum.SortOrder.LayoutOrder
        layout.HorizontalAlignment = Enum.HorizontalAlignment.Center
        layout.Padding = UDim.new(0, 0)
        layout.Parent = c
        return c
    end

    local function makeSection(parent, label, order)
        local f = Instance.new("Frame")
        f.Size = UDim2.new(1, 0, 0, 18)
        f.BackgroundTransparency = 1
        f.LayoutOrder = order
        f.Parent = parent
        local lbl = Instance.new("TextLabel")
        lbl.Size = UDim2.new(1, 0, 1, 0)
        lbl.Text = string.upper(label)
        lbl.Font = Enum.Font.GothamBold
        lbl.TextSize = 8
        lbl.TextColor3 = accent
        lbl.TextXAlignment = Enum.TextXAlignment.Left
        lbl.BackgroundTransparency = 1
        lbl.Parent = f
        local line = Instance.new("Frame")
        line.Size = UDim2.new(0, 22, 0, 2)
        line.Position = UDim2.new(0, 0, 1, -2)
        line.BackgroundColor3 = accent
        line.BorderSizePixel = 0
        line.Parent = f
        Instance.new("UICorner", line).CornerRadius = UDim.new(1, 0)
    end

    local function makeRow(card, order, isLast, h)
        local row = Instance.new("Frame")
        row.Size = UDim2.new(1, 0, 0, h or 38)
        row.BackgroundTransparency = 1
        row.LayoutOrder = order
        row.Parent = card
        if not isLast then
            local sep = Instance.new("Frame")
            sep.Size = UDim2.new(1, -20, 0, 1)
            sep.Position = UDim2.new(0, 10, 1, -1)
            sep.BackgroundColor3 = T.sep
            sep.BorderSizePixel = 0
            sep.Parent = row
        end
        return row
    end

    -- Improved Dropdown (always shows all options, positioned correctly)
    local function addDropdown(parent, text, options, getter, setter, order)
        local frame = Instance.new("Frame")
        frame.Size = UDim2.new(1, 0, 0, 40)
        frame.BackgroundColor3 = T.card
        frame.LayoutOrder = order
        frame.Parent = parent
        Instance.new("UICorner", frame).CornerRadius = UDim.new(0, 10)

        local label = Instance.new("TextLabel")
        label.Size = UDim2.new(0.5, -10, 1, 0)
        label.Position = UDim2.new(0, 8, 0, 0)
        label.Text = text
        label.Font = Enum.Font.GothamSemibold
        label.TextSize = 10
        label.TextColor3 = T.text
        label.TextXAlignment = Enum.TextXAlignment.Left
        label.BackgroundTransparency = 1
        label.Parent = frame

        local drop = Instance.new("TextButton")
        drop.Size = UDim2.new(0.4, 0, 0, 26)
        drop.Position = UDim2.new(0.55, 0, 0, 7)
        drop.Text = getter()
        drop.Font = Enum.Font.Gotham
        drop.TextSize = 10
        drop.BackgroundColor3 = T.dim
        drop.TextColor3 = T.text
        drop.BorderSizePixel = 0
        drop.Parent = frame
        Instance.new("UICorner", drop).CornerRadius = UDim.new(0, 4)

        local menuOpen = false
        local menu = nil
        drop.MouseButton1Click:Connect(function()
            if menuOpen then return end
            menuOpen = true
            local absPos = drop.AbsolutePosition
            local menuHeight = #options * 26
            menu = Instance.new("Frame")
            menu.Size = UDim2.new(0, 140, 0, menuHeight)
            menu.Position = UDim2.new(0, absPos.X + 5, 0, absPos.Y + 26)
            menu.BackgroundColor3 = T.card
            menu.BorderSizePixel = 0
            menu.ZIndex = 200
            menu.Parent = screenGui
            Instance.new("UICorner", menu).CornerRadius = UDim.new(0, 6)
            local menuStroke = Instance.new("UIStroke")
            menuStroke.Color = T.border
            menuStroke.Thickness = 1
            menuStroke.Parent = menu

            -- Scrollable container in case many options
            local scroll = Instance.new("ScrollingFrame")
            scroll.Size = UDim2.new(1, 0, 1, 0)
            scroll.BackgroundTransparency = 1
            scroll.BorderSizePixel = 0
            scroll.CanvasSize = UDim2.new(0, 0, 0, menuHeight)
            scroll.AutomaticCanvasSize = Enum.AutomaticSize.Y
            scroll.ScrollBarThickness = 4
            scroll.Parent = menu
            local layout = Instance.new("UIListLayout")
            layout.SortOrder = Enum.SortOrder.LayoutOrder
            layout.Padding = UDim.new(0, 1)
            layout.Parent = scroll

            for _, opt in ipairs(options) do
                local btn = Instance.new("TextButton")
                btn.Size = UDim2.new(1, 0, 0, 26)
                btn.Text = opt
                btn.Font = Enum.Font.Gotham
                btn.TextSize = 10
                btn.BackgroundColor3 = T.dim
                btn.TextColor3 = T.text
                btn.BorderSizePixel = 0
                btn.Parent = scroll
                btn.MouseButton1Click:Connect(function()
                    setter(opt)
                    drop.Text = opt
                    menu:Destroy()
                    menuOpen = false
                end)
            end

            local function closeMenu()
                if menu then menu:Destroy() end
                menuOpen = false
            end
            -- Close when clicking outside
            local clickConn
            clickConn = UserInputService.InputBegan:Connect(function(inp)
                if inp.UserInputType == Enum.UserInputType.MouseButton1 then
                    local mousePos = inp.Position
                    if menu and menu.Parent then
                        local mAbs = menu.AbsolutePosition
                        local mSize = menu.AbsoluteSize
                        if mousePos.X < mAbs.X or mousePos.X > mAbs.X + mSize.X or mousePos.Y < mAbs.Y or mousePos.Y > mAbs.Y + mSize.Y then
                            closeMenu()
                            clickConn:Disconnect()
                        end
                    else
                        clickConn:Disconnect()
                    end
                end
            end)
        end)
        return frame
    end

    local function addSlider(parent, labelText, minVal, maxVal, getter, setter, order)
        local frame = Instance.new("Frame")
        frame.Size = UDim2.new(1, 0, 0, 50)
        frame.BackgroundColor3 = T.card
        frame.LayoutOrder = order
        frame.Parent = parent
        Instance.new("UICorner", frame).CornerRadius = UDim.new(0, 10)

        local label = Instance.new("TextLabel")
        label.Size = UDim2.new(1, -10, 0, 18)
        label.Position = UDim2.new(0, 8, 0, 4)
        label.Text = labelText .. ": " .. tostring(getter())
        label.Font = Enum.Font.GothamSemibold
        label.TextSize = 10
        label.TextColor3 = T.text
        label.BackgroundTransparency = 1
        label.TextXAlignment = Enum.TextXAlignment.Left
        label.Parent = frame

        local decBtn = Instance.new("TextButton")
        decBtn.Size = UDim2.new(0, 28, 0, 24)
        decBtn.Position = UDim2.new(1, -76, 0, 22)
        decBtn.Text = "-"
        decBtn.Font = Enum.Font.GothamBold
        decBtn.TextSize = 14
        decBtn.BackgroundColor3 = T.dim
        decBtn.TextColor3 = T.text
        decBtn.BorderSizePixel = 0
        decBtn.Parent = frame
        Instance.new("UICorner", decBtn).CornerRadius = UDim.new(0, 4)

        local valLabel = Instance.new("TextLabel")
        valLabel.Size = UDim2.new(0, 40, 0, 24)
        valLabel.Position = UDim2.new(1, -46, 0, 22)
        valLabel.Text = tostring(getter())
        valLabel.Font = Enum.Font.GothamBold
        valLabel.TextSize = 10
        valLabel.BackgroundColor3 = T.bg
        valLabel.TextColor3 = T.text
        valLabel.BorderSizePixel = 0
        valLabel.Parent = frame
        Instance.new("UICorner", valLabel).CornerRadius = UDim.new(0, 4)

        local incBtn = Instance.new("TextButton")
        incBtn.Size = UDim2.new(0, 28, 0, 24)
        incBtn.Position = UDim2.new(1, -116, 0, 22)
        incBtn.Text = "+"
        incBtn.Font = Enum.Font.GothamBold
        incBtn.TextSize = 14
        incBtn.BackgroundColor3 = T.dim
        incBtn.TextColor3 = T.text
        incBtn.BorderSizePixel = 0
        incBtn.Parent = frame
        Instance.new("UICorner", incBtn).CornerRadius = UDim.new(0, 4)

        local function update()
            local val = getter()
            label.Text = labelText .. ": " .. string.format("%.1f", val)
            valLabel.Text = string.format("%.1f", val)
        end

        decBtn.MouseButton1Click:Connect(function()
            local newVal = math.max(minVal, getter() - (maxVal - minVal) / 20)
            setter(newVal)
            update()
        end)
        incBtn.MouseButton1Click:Connect(function()
            local newVal = math.min(maxVal, getter() + (maxVal - minVal) / 20)
            setter(newVal)
            update()
        end)
        return frame
    end

    -- Build Pages
    -- Page 1: Aimbot
    local pgAim = pages[1]
    makeSection(pgAim, "Aimbot", 1)
    local card1 = makeCard(pgAim, 2)

    local function updateAimbotToggleUI()
        if uiElements.aimbotToggle then
            uiElements.aimbotToggle.Text = settings.aimbotEnabled and "ON" or "OFF"
            uiElements.aimbotToggle.BackgroundColor3 = settings.aimbotEnabled and Color3.fromRGB(80, 180, 80) or Color3.fromRGB(180, 60, 60)
        end
    end

    -- Custom toggle row (so we can store reference)
    local aimRow = makeRow(card1, 1, false, 38)
    local aimLabel = Instance.new("TextLabel")
    aimLabel.Size = UDim2.new(1, -60, 1, 0)
    aimLabel.Position = UDim2.new(0, 12, 0, 0)
    aimLabel.Text = "Enable Aimbot"
    aimLabel.Font = Enum.Font.GothamSemibold
    aimLabel.TextSize = 10
    aimLabel.TextColor3 = T.text
    aimLabel.TextXAlignment = Enum.TextXAlignment.Left
    aimLabel.BackgroundTransparency = 1
    aimLabel.Parent = aimRow

    local aimToggleBg = Instance.new("Frame")
    aimToggleBg.Size = UDim2.new(0, 36, 0, 18)
    aimToggleBg.Position = UDim2.new(1, -48, 0.5, -9)
    aimToggleBg.BackgroundColor3 = settings.aimbotEnabled and accent or T.dim
    aimToggleBg.Parent = aimRow
    Instance.new("UICorner", aimToggleBg).CornerRadius = UDim.new(1, 0)
    local aimToggleStroke = Instance.new("UIStroke")
    aimToggleStroke.Color = T.border
    aimToggleStroke.Thickness = 1
    aimToggleStroke.Parent = aimToggleBg

    local aimToggleCircle = Instance.new("Frame")
    aimToggleCircle.Size = UDim2.new(0, 14, 0, 14)
    aimToggleCircle.Position = settings.aimbotEnabled and UDim2.new(1, -16, 0.5, -7) or UDim2.new(0, 2, 0.5, -7)
    aimToggleCircle.BackgroundColor3 = Color3.new(1, 1, 1)
    aimToggleCircle.Parent = aimToggleBg
    Instance.new("UICorner", aimToggleCircle).CornerRadius = UDim.new(1, 0)

    local aimClick = Instance.new("TextButton")
    aimClick.Size = UDim2.new(0, 50, 1, 0)
    aimClick.Position = UDim2.new(1, -56, 0, 0)
    aimClick.BackgroundTransparency = 1
    aimClick.Text = ""
    aimClick.Parent = aimRow

    aimClick.MouseButton1Click:Connect(function()
        settings.aimbotEnabled = not settings.aimbotEnabled
        TweenService:Create(aimToggleBg, tw(0.25), {BackgroundColor3 = settings.aimbotEnabled and accent or T.dim}):Play()
        TweenService:Create(aimToggleCircle, tw(0.25, Enum.EasingStyle.Back), {Position = settings.aimbotEnabled and UDim2.new(1, -16, 0.5, -7) or UDim2.new(0, 2, 0.5, -7)}):Play()
        updateAimbotToggleUI()
    end)
    uiElements.aimbotToggle = aimClick

    addDropdown(card1, "Target Priority", {"mouse", "player"}, function() return settings.targetMode end, function(v) settings.targetMode = v end, 2)
    addSlider(card1, "FOV Distance (studs)", 50, 500, function() return settings.fovDistance end, function(v) settings.fovDistance = v end, 3)
    addSlider(card1, "Smoothing (0=instant)", 0, 0.95, function() return settings.smoothing end, function(v) settings.smoothing = v end, 4)
    addDropdown(card1, "Aim Part", {"Head", "HumanoidRootPart"}, function() return settings.aimPart end, function(v) settings.aimPart = v end, 5)

    -- Keybind row
    local keyRow = makeRow(card1, 6, true, 40)
    local keyLabel = Instance.new("TextLabel")
    keyLabel.Size = UDim2.new(0.6, -10, 1, 0)
    keyLabel.Position = UDim2.new(0, 12, 0, 0)
    keyLabel.Text = "Aimbot Keybind"
    keyLabel.Font = Enum.Font.GothamSemibold
    keyLabel.TextSize = 10
    keyLabel.TextColor3 = T.text
    keyLabel.TextXAlignment = Enum.TextXAlignment.Left
    keyLabel.BackgroundTransparency = 1
    keyLabel.Parent = keyRow

    local keyButton = Instance.new("TextButton")
    keyButton.Size = UDim2.new(0.3, 0, 0, 26)
    keyButton.Position = UDim2.new(0.65, 0, 0, 7)
    keyButton.Text = settings.aimKey.Name
    keyButton.Font = Enum.Font.GothamBold
    keyButton.TextSize = 10
    keyButton.BackgroundColor3 = T.dim
    keyButton.TextColor3 = T.text
    keyButton.BorderSizePixel = 0
    keyButton.Parent = keyRow
    Instance.new("UICorner", keyButton).CornerRadius = UDim.new(0, 4)
    uiElements.keyButton = keyButton

    keyButton.MouseButton1Click:Connect(function()
        waitingForKeybind = true
        keyButton.Text = "..."
        task.wait(0.1)
        while waitingForKeybind do task.wait() end
        keyButton.Text = settings.aimKey.Name
    end)

    -- Page 2: ESP
    local pgEsp = pages[2]
    makeSection(pgEsp, "ESP", 1)
    local espCard = makeCard(pgEsp, 2)

    local function updateESPUI()
        if uiElements.espToggle then
            uiElements.espToggle.Text = settings.espEnabled and "ON" or "OFF"
            uiElements.espToggle.BackgroundColor3 = settings.espEnabled and Color3.fromRGB(80, 180, 80) or Color3.fromRGB(180, 60, 60)
        end
    end

    local espRow = makeRow(espCard, 1, false, 38)
    local espLabel = Instance.new("TextLabel")
    espLabel.Size = UDim2.new(1, -60, 1, 0)
    espLabel.Position = UDim2.new(0, 12, 0, 0)
    espLabel.Text = "Enable ESP"
    espLabel.Font = Enum.Font.GothamSemibold
    espLabel.TextSize = 10
    espLabel.TextColor3 = T.text
    espLabel.TextXAlignment = Enum.TextXAlignment.Left
    espLabel.BackgroundTransparency = 1
    espLabel.Parent = espRow

    local espToggleBg = Instance.new("Frame")
    espToggleBg.Size = UDim2.new(0, 36, 0, 18)
    espToggleBg.Position = UDim2.new(1, -48, 0.5, -9)
    espToggleBg.BackgroundColor3 = settings.espEnabled and accent or T.dim
    espToggleBg.Parent = espRow
    Instance.new("UICorner", espToggleBg).CornerRadius = UDim.new(1, 0)
    local espToggleStroke = Instance.new("UIStroke")
    espToggleStroke.Color = T.border
    espToggleStroke.Thickness = 1
    espToggleStroke.Parent = espToggleBg

    local espToggleCircle = Instance.new("Frame")
    espToggleCircle.Size = UDim2.new(0, 14, 0, 14)
    espToggleCircle.Position = settings.espEnabled and UDim2.new(1, -16, 0.5, -7) or UDim2.new(0, 2, 0.5, -7)
    espToggleCircle.BackgroundColor3 = Color3.new(1, 1, 1)
    espToggleCircle.Parent = espToggleBg
    Instance.new("UICorner", espToggleCircle).CornerRadius = UDim.new(1, 0)

    local espClick = Instance.new("TextButton")
    espClick.Size = UDim2.new(0, 50, 1, 0)
    espClick.Position = UDim2.new(1, -56, 0, 0)
    espClick.BackgroundTransparency = 1
    espClick.Text = ""
    espClick.Parent = espRow

    espClick.MouseButton1Click:Connect(function()
        settings.espEnabled = not settings.espEnabled
        TweenService:Create(espToggleBg, tw(0.25), {BackgroundColor3 = settings.espEnabled and accent or T.dim}):Play()
        TweenService:Create(espToggleCircle, tw(0.25, Enum.EasingStyle.Back), {Position = settings.espEnabled and UDim2.new(1, -16, 0.5, -7) or UDim2.new(0, 2, 0.5, -7)}):Play()
        updateESP()
        updateESPUI()
    end)
    uiElements.espToggle = espClick

    -- Color preset dropdown
    local colorOptions = {"White", "Red", "Green", "Blue", "Yellow", "Purple", "Orange"}
    addDropdown(espCard, "ESP Color", colorOptions, function() return settings.espColorPreset end, function(v)
        settings.espColorPreset = v
        settings.espColor = colorPresets[v]
        updateESP()
    end, 2)

    addSlider(espCard, "ESP Transparency", 0, 1, function() return settings.espTransparency end, function(v)
        settings.espTransparency = v
        updateESP()
    end, 3)

    -- Tracers toggle
    local function updateTracersUI()
        if uiElements.tracersToggle then
            uiElements.tracersToggle.Text = settings.tracersEnabled and "ON" or "OFF"
            uiElements.tracersToggle.BackgroundColor3 = settings.tracersEnabled and Color3.fromRGB(80, 180, 80) or Color3.fromRGB(180, 60, 60)
        end
    end

    local tracersRow = makeRow(espCard, 4, false, 38)
    local tracersLabel = Instance.new("TextLabel")
    tracersLabel.Size = UDim2.new(1, -60, 1, 0)
    tracersLabel.Position = UDim2.new(0, 12, 0, 0)
    tracersLabel.Text = "Tracers"
    tracersLabel.Font = Enum.Font.GothamSemibold
    tracersLabel.TextSize = 10
    tracersLabel.TextColor3 = T.text
    tracersLabel.TextXAlignment = Enum.TextXAlignment.Left
    tracersLabel.BackgroundTransparency = 1
    tracersLabel.Parent = tracersRow

    local tracersToggleBg = Instance.new("Frame")
    tracersToggleBg.Size = UDim2.new(0, 36, 0, 18)
    tracersToggleBg.Position = UDim2.new(1, -48, 0.5, -9)
    tracersToggleBg.BackgroundColor3 = settings.tracersEnabled and accent or T.dim
    tracersToggleBg.Parent = tracersRow
    Instance.new("UICorner", tracersToggleBg).CornerRadius = UDim.new(1, 0)
    local tracersToggleStroke = Instance.new("UIStroke")
    tracersToggleStroke.Color = T.border
    tracersToggleStroke.Thickness = 1
    tracersToggleStroke.Parent = tracersToggleBg

    local tracersToggleCircle = Instance.new("Frame")
    tracersToggleCircle.Size = UDim2.new(0, 14, 0, 14)
    tracersToggleCircle.Position = settings.tracersEnabled and UDim2.new(1, -16, 0.5, -7) or UDim2.new(0, 2, 0.5, -7)
    tracersToggleCircle.BackgroundColor3 = Color3.new(1, 1, 1)
    tracersToggleCircle.Parent = tracersToggleBg
    Instance.new("UICorner", tracersToggleCircle).CornerRadius = UDim.new(1, 0)

    local tracersClick = Instance.new("TextButton")
    tracersClick.Size = UDim2.new(0, 50, 1, 0)
    tracersClick.Position = UDim2.new(1, -56, 0, 0)
    tracersClick.BackgroundTransparency = 1
    tracersClick.Text = ""
    tracersClick.Parent = tracersRow

    tracersClick.MouseButton1Click:Connect(function()
        settings.tracersEnabled = not settings.tracersEnabled
        TweenService:Create(tracersToggleBg, tw(0.25), {BackgroundColor3 = settings.tracersEnabled and accent or T.dim}):Play()
        TweenService:Create(tracersToggleCircle, tw(0.25, Enum.EasingStyle.Back), {Position = settings.tracersEnabled and UDim2.new(1, -16, 0.5, -7) or UDim2.new(0, 2, 0.5, -7)}):Play()
        updateESP()
        updateTracersUI()
    end)
    uiElements.tracersToggle = tracersClick

    -- Highlights toggle
    local function updateHighlightsUI()
        if uiElements.highlightsToggle then
            uiElements.highlightsToggle.Text = settings.highlightsEnabled and "ON" or "OFF"
            uiElements.highlightsToggle.BackgroundColor3 = settings.highlightsEnabled and Color3.fromRGB(80, 180, 80) or Color3.fromRGB(180, 60, 60)
        end
    end

    local highlightsRow = makeRow(espCard, 5, true, 38)
    local highlightsLabel = Instance.new("TextLabel")
    highlightsLabel.Size = UDim2.new(1, -60, 1, 0)
    highlightsLabel.Position = UDim2.new(0, 12, 0, 0)
    highlightsLabel.Text = "Highlights"
    highlightsLabel.Font = Enum.Font.GothamSemibold
    highlightsLabel.TextSize = 10
    highlightsLabel.TextColor3 = T.text
    highlightsLabel.TextXAlignment = Enum.TextXAlignment.Left
    highlightsLabel.BackgroundTransparency = 1
    highlightsLabel.Parent = highlightsRow

    local highlightsToggleBg = Instance.new("Frame")
    highlightsToggleBg.Size = UDim2.new(0, 36, 0, 18)
    highlightsToggleBg.Position = UDim2.new(1, -48, 0.5, -9)
    highlightsToggleBg.BackgroundColor3 = settings.highlightsEnabled and accent or T.dim
    highlightsToggleBg.Parent = highlightsRow
    Instance.new("UICorner", highlightsToggleBg).CornerRadius = UDim.new(1, 0)
    local highlightsToggleStroke = Instance.new("UIStroke")
    highlightsToggleStroke.Color = T.border
    highlightsToggleStroke.Thickness = 1
    highlightsToggleStroke.Parent = highlightsToggleBg

    local highlightsToggleCircle = Instance.new("Frame")
    highlightsToggleCircle.Size = UDim2.new(0, 14, 0, 14)
    highlightsToggleCircle.Position = settings.highlightsEnabled and UDim2.new(1, -16, 0.5, -7) or UDim2.new(0, 2, 0.5, -7)
    highlightsToggleCircle.BackgroundColor3 = Color3.new(1, 1, 1)
    highlightsToggleCircle.Parent = highlightsToggleBg
    Instance.new("UICorner", highlightsToggleCircle).CornerRadius = UDim.new(1, 0)

    local highlightsClick = Instance.new("TextButton")
    highlightsClick.Size = UDim2.new(0, 50, 1, 0)
    highlightsClick.Position = UDim2.new(1, -56, 0, 0)
    highlightsClick.BackgroundTransparency = 1
    highlightsClick.Text = ""
    highlightsClick.Parent = highlightsRow

    highlightsClick.MouseButton1Click:Connect(function()
        settings.highlightsEnabled = not settings.highlightsEnabled
        TweenService:Create(highlightsToggleBg, tw(0.25), {BackgroundColor3 = settings.highlightsEnabled and accent or T.dim}):Play()
        TweenService:Create(highlightsToggleCircle, tw(0.25, Enum.EasingStyle.Back), {Position = settings.highlightsEnabled and UDim2.new(1, -16, 0.5, -7) or UDim2.new(0, 2, 0.5, -7)}):Play()
        updateESP()
        updateHighlightsUI()
    end)
    uiElements.highlightsToggle = highlightsClick

    -- Page 3: Settings
    local pgSet = pages[3]
    makeSection(pgSet, "Info", 1)
    local infoCard = makeCard(pgSet, 2)

    local infoLabel = Instance.new("TextLabel")
    infoLabel.Size = UDim2.new(1, -16, 0, 40)
    infoLabel.Position = UDim2.new(0, 8, 0, 8)
    infoLabel.Text = "Open/close UI:  ]\nToggle aimbot:  " .. settings.aimKey.Name
    infoLabel.Font = Enum.Font.Gotham
    infoLabel.TextSize = 9
    infoLabel.TextColor3 = T.muted
    infoLabel.TextWrapped = true
    infoLabel.BackgroundTransparency = 1
    infoLabel.Parent = infoCard
    uiElements.infoLabel = infoLabel

    local resetBtn = Instance.new("TextButton")
    resetBtn.Size = UDim2.new(1, -16, 0, 30)
    resetBtn.Position = UDim2.new(0, 8, 0, 52)
    resetBtn.Text = "Reset to Defaults"
    resetBtn.Font = Enum.Font.GothamBold
    resetBtn.TextSize = 10
    resetBtn.BackgroundColor3 = Color3.fromRGB(180, 60, 60)
    resetBtn.TextColor3 = Color3.new(1, 1, 1)
    resetBtn.BorderSizePixel = 0
    resetBtn.Parent = infoCard
    Instance.new("UICorner", resetBtn).CornerRadius = UDim.new(0, 6)

    resetBtn.MouseButton1Click:Connect(function()
        settings.aimbotEnabled = false
        settings.targetMode = "mouse"
        settings.fovDistance = 200
        settings.smoothing = 0
        settings.aimPart = "Head"
        settings.espEnabled = true
        settings.espColorPreset = "White"
        settings.espColor = Color3.fromRGB(255, 255, 255)
        settings.espTransparency = 0.6
        settings.tracersEnabled = true
        settings.highlightsEnabled = true
        updateESP()
        createUI()
    end)

    -- Animation
    win.Size = UDim2.new(0, 0, 0, 0)
    win.BackgroundTransparency = 1
    TweenService:Create(win, tw(0.4, Enum.EasingStyle.Back, Enum.EasingDirection.Out), {Size = UDim2.new(0, WIN_W, 0, WIN_H), BackgroundTransparency = 0}):Play()
    setTab(1)

    -- Glow pulse
    task.spawn(function()
        while screenGui and screenGui.Parent do
            TweenService:Create(winGlow, TweenInfo.new(2.4, Enum.EasingStyle.Sine, Enum.EasingDirection.InOut), {ImageTransparency = 0.82}):Play()
            task.wait(2.4)
            TweenService:Create(winGlow, TweenInfo.new(2.4, Enum.EasingStyle.Sine, Enum.EasingDirection.InOut), {ImageTransparency = 0.92}):Play()
            task.wait(2.4)
        end
    end)
end

createUI()
print("Flick Aim+ESP v1 loaded. Press ] to open UI, E to toggle aimbot.")
